'use strict';

describe('projectBoard.sprints module', function() {

  beforeEach(module('projectBoard.sprints'));

  describe('sprints controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SprintsCtrl = $controller('SprintsCtrl');
      expect(SprintsCtrl).toBeDefined();
      //spec body
      var SprintDetailCtrl = $controller('SprintDetailCtrl');
      expect(SprintDetailCtrl).toBeDefined();
    }));

  });
});

