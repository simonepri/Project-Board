'use strict';

describe('projectBoard.resources module', function() {

  beforeEach(module('projectBoard.resources'));

  describe('resources controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ResourcesCtrl = $controller('ResourcesCtrl');
      expect(ResourcesCtrl).toBeDefined();
    }));

  });
});
